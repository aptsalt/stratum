{
  "name": "stratum",
  "private": true,
  "description": "Layered agent-graph studio for Google ADK 2.x — Angular 21 UI + FastAPI/ADK runtime",
  "scripts": {
    "setup": "cd backend && python -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt && cd ../frontend && npm install",
    "api": "cd backend && .venv/Scripts/python -m uvicorn app:app --reload --port 8000",
    "web": "npm --prefix frontend start",
    "test": "cd backend && .venv/Scripts/python -m pytest -q tests",
    "build": "npm --prefix frontend run build"
  }
}
